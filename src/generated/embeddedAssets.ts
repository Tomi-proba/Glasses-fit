// Placeholder — real content is generated on demand by
// scripts/generate-embedded-assets.mjs (run via `npm run build:standalone`)
// and is never committed, since it's ~20MB of base64. This stub only
// exists so the normal app build (which never actually executes the code
// path that imports this) has a real module to resolve at build time.
export const WASM_JS_B64 = '';
export const WASM_BINARY_B64 = '';
export const MODEL_B64 = '';
